{
  "name": "investing-reports",
  "version": "1.0.0",
  "description": "投资分析报告站点",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "node scripts/generate-pages.cjs && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "generate": "node scripts/generate-pages.cjs"
  },
  "devDependencies": {
    "js-yaml": "^4.1.1",
    "vitepress": "^1.6.3"
  }
}
